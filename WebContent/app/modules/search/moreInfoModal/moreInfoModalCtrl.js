angular.module('searchModule').controller('moreInfoModalCtrl', function($scope, items) {
    $scope.Data = {
        items: items
    };

    $scope.Func = {

    };

    var Run = function(){

    };

    Run();
});
